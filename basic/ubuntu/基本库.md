# 高版本的基本库，不要安装在/usr里，指定安装路径

先确定好opencv，再装ros
#### Cmake升级
```bash
sudo apt-get update
sudo apt-get -y install software-properties-common lsb-release
sudo apt-add-repository "deb https://apt.kitware.com/ubuntu/ $(lsb_release -cs) main"
```

`wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc | sudo apt-key add -`

`sudo apt-get update`
```bash
sudo apt-get install cmake
```

临时切换版本
```
export PATH-/usr/local/cmake-3.10/bin:$PATH
```
#### gcc/g++
```bash
sudo add-apt-repository ppa:ubuntu-toolchain-r/test
sudo apt install g++-11 gcc-11

sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-11 110
sudo update-alternatives --condif gcc
```

#### boost
jenson的boost在
/usr/incluse和/usr/lib/aarch64-linux-gnu下

在官网下载源码
```bash
# 可能会默认链接到python2,所以指定使用python3
./bootstrap.sh --with-python=/usr/bin/python3 --prefix=/usr/local
```
pcl依赖boost
gtsam依赖boost