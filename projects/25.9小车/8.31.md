OrbbecSDK ROS2会有bug,但是 ros2_astra_camera可以正常使用，怀疑是OrbbecSDK ROS2更多适配新相机，ros2_astra_camera更老，反而可以正常使用。

ros2_astra_camera提供的例子可以直接读取图像，花了一些时间做了一个简单的深度检测，主要是ros2和1很多地方不一样。

雷达的使用也提供了接口，不过 也有坑，端口号不是默认的，需要修改。9966和8877
目前已经可以得到点云了。

slam算法有好多依赖库的问题，先不在ros2上部署了，直接在tx2上部署

这个tx2要重新烧录。