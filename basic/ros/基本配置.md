

- ros2 编译
colcon build --packages-select read_depth_image

- ROS_PACKAGE_PATH找不到的情况
  很有可能是因为在 ~/.bashrc 中 source 了的包被删除后，ROS_PACKAGE_PATH 就会产生混乱。
  尤其是在一个目录下的两个包，相互间会有影响。比如~/project/1_ws  和 ~/project/2_ws 会有影响，最好在根目录中分别创建。